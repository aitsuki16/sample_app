<%= form_with(model: current_user.active_relationships.build) do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>  <!-- Pass the user being followed -->
  <%= f.submit "Follow", class: "btn btn-primary" %>  <!-- Follow button -->
<% end %>