# <%= turbo_stream.update "follow_form" do %>
#   <%= render partial: "users/unfollow" %>
# <% end %>
# <%= turbo_stream.update "followers" do %>
#   <%= @user.followers.count %>
# <% end %>
  
<%= turbo_stream.replace "follow_form", partial: "users/follow", locals: { user: @user } %>
<%= turbo_stream.replace "followers", partial: "shared/followers_count", locals: { user: @user } %>
  